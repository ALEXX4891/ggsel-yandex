/**
 * Партнерский API Маркета
 * API Яндекс Маркета помогает продавцам автоматизировать и упростить работу с маркетплейсом.  В числе возможностей интеграции:  * управление каталогом товаров и витриной,  * обработка заказов,  * изменение настроек магазина,  * получение отчетов. 
 *
 * The version of the OpenAPI document: LATEST
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ForwardScrollingPagerDTO from './ForwardScrollingPagerDTO';
import WarehouseDetailsDTO from './WarehouseDetailsDTO';

/**
 * The PagedWarehousesDTO model module.
 * @module model/PagedWarehousesDTO
 * @version LATEST
 */
class PagedWarehousesDTO {
    /**
     * Constructs a new <code>PagedWarehousesDTO</code>.
     * Информация о складах в кабинете.
     * @alias module:model/PagedWarehousesDTO
     * @param warehouses {Array.<module:model/WarehouseDetailsDTO>} Список складов.
     */
    constructor(warehouses) { 
        
        PagedWarehousesDTO.initialize(this, warehouses);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, warehouses) { 
        obj['warehouses'] = warehouses;
    }

    /**
     * Constructs a <code>PagedWarehousesDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PagedWarehousesDTO} obj Optional instance to populate.
     * @return {module:model/PagedWarehousesDTO} The populated <code>PagedWarehousesDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PagedWarehousesDTO();

            if (data.hasOwnProperty('warehouses')) {
                obj['warehouses'] = ApiClient.convertToType(data['warehouses'], [WarehouseDetailsDTO]);
            }
            if (data.hasOwnProperty('paging')) {
                obj['paging'] = ForwardScrollingPagerDTO.constructFromObject(data['paging']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PagedWarehousesDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PagedWarehousesDTO</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PagedWarehousesDTO.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['warehouses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['warehouses'])) {
                throw new Error("Expected the field `warehouses` to be an array in the JSON data but got " + data['warehouses']);
            }
            // validate the optional field `warehouses` (array)
            for (const item of data['warehouses']) {
                WarehouseDetailsDTO.validateJSON(item);
            };
        }
        // validate the optional field `paging`
        if (data['paging']) { // data not null
          ForwardScrollingPagerDTO.validateJSON(data['paging']);
        }

        return true;
    }


}

PagedWarehousesDTO.RequiredProperties = ["warehouses"];

/**
 * Список складов.
 * @member {Array.<module:model/WarehouseDetailsDTO>} warehouses
 */
PagedWarehousesDTO.prototype['warehouses'] = undefined;

/**
 * @member {module:model/ForwardScrollingPagerDTO} paging
 */
PagedWarehousesDTO.prototype['paging'] = undefined;






export default PagedWarehousesDTO;

